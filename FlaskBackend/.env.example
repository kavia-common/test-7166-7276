# MongoDB connection settings
MONGO_URI=mongodb://localhost:27017
DB_NAME=devices_db
COLLECTION_NAME=devices
# Optional audit collection name (defaults to 'audit_logs' if not set)
AUDIT_COLLECTION_NAME=audit_logs

# Flask server settings (optional)
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
